import React from 'react';
import styles from '../../styles/Banner.module.css';

const Banner = ({data}) => {
  return(
    <div className={styles.Banner}> Banner Area </div>
  )
}

export default Banner;
